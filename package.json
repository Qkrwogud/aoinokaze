{
  "name": "threecx-queue-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local 3CX queue status dashboard with elapsed login and logout timers",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
